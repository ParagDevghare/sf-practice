
<aura:component implements="c:FlowAttributes">
  <lightning:navigation aura:id="navservice"/>
  <lightning:workspaceAPI aura:id="workspace"/>
  <!--TODO : This will load when component loads first time-->
  <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
  <div class="slds-card">
      {!v.body}
  </div>
</aura:component>	
